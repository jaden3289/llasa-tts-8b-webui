# Core dependencies (torch 2.6.0 from base image - pin to prevent downgrade)
torch==2.6.0
torchaudio==2.6.0
transformers==4.45.2
accelerate>=0.25.0

# Gradio for Web UI
gradio>=4.0.0

# Audio processing
faster-whisper>=0.10.0

# API dependencies
flask==3.0.0
flask-cors==4.0.0
flasgger==0.9.7.1

# MCP dependencies
fastmcp
pydantic>=2.5.0

# Utilities
huggingface_hub>=0.20.0
safetensors>=0.4.0
